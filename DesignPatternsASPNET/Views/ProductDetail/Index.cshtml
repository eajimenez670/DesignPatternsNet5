@{
    decimal totalLocal = (decimal)ViewBag.totalLocal;
    decimal totalForeign = (decimal)ViewBag.totalForeign;
}

<div class="row">
    <div class="col-12">
        <span>Total local: <strong>@totalLocal</strong></span>
    </div>
</div>
<div class="row">
    <div class="col-12">
        <span>Total foreign: <strong>@totalForeign</strong></span>
    </div>
</div>
